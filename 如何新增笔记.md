# 📝 如何新增笔记（操作指南）

> 本站基于 docsify：**纯静态、无需编译**，添加一篇笔记只需两步——放文件 + 加导航。

## 一、日常新增笔记（3 步）

### 第 1 步：把 Markdown 文件放进对应栏目

```
C:\book\docs\
├── 随笔\            ← 生活感悟、杂谈放这里
├── 读书笔记\        ← 读书笔记放这里
└── 公考笔记\
    ├── 判断推理\    ← 判断推理的新笔记放这里
    └── 资料分析\    ← 资料分析的新笔记放这里
```

例如新写了一篇《数量关系入门.md》，想新开一个"数量关系"栏目：

```
C:\book\docs\公考笔记\数量关系\01-数量关系入门.md
```

> 文件第一行写 `# 标题`，docsify 会自动渲染为页面标题。

### 第 2 步：在 `_sidebar.md` 中加一行导航

打开 `C:\book\_sidebar.md`，在对应分组下按格式加一行：

```markdown
* **🎯 公考笔记**
  * 数量关系                              ← 新栏目（纯文字 = 分组标题）
    * [数量关系入门](docs/公考笔记/数量关系/01-数量关系入门.md)   ← 新文章
```

（可选）如果想让首页 `README.md` 或顶部导航 `_navbar.md` 也出现入口，同样加一行链接即可。

### 第 3 步：本地预览确认

```bash
# 在 C:\book 目录下启动预览（禁缓存，改完刷新即见）
python serve.py 4600
# 浏览器打开 http://127.0.0.1:4600/
```

确认无误后，推送到 GitHub（见下一节）。

## 二、发布到线上（推送 GitHub）

在 `C:\book` 目录下执行三条命令：

```bash
git add .
git commit -m "新增笔记：xxx"
git push
```

推送后约 1 分钟，GitHub Pages 外链自动更新，无需其他操作。

## 三、常见问题

| 问题 | 解决 |
|------|------|
| 新文章打开是 404 | 检查 `_sidebar.md` 里的路径和实际文件路径是否一致（含中文、大小写） |
| 改了内容浏览器没变化 | 用 `serve.py` 预览；线上则 Ctrl+Shift+R 强制刷新 |
| 想新开一个大模块（如"技术"） | 在 `docs/` 下建新文件夹，`_sidebar.md` 加一个 `* **模块名**` 分组 |
| 图片怎么放 | 建议在栏目下建 `images/` 文件夹，md 里用相对路径引用 |

## 四、目录职责速查

| 文件 | 作用 | 什么时候动它 |
|------|------|-------------|
| `docs/**/*.md` | 文章正文 | 每次写新笔记 |
| `_sidebar.md` | 左侧导航 | 每次新增文章/栏目 |
| `_navbar.md` | 顶部导航 | 新增大模块时（可选） |
| `README.md` | 博客首页 | 想在首页推荐文章时（可选） |
| `_coverpage.md` | 封面 | 基本不动 |
| `index.html` | 全局配置/样式 | 基本不动 |
